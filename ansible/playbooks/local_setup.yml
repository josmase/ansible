---
# Local Development Environment Setup Playbook
# This playbook configures the local development environment including:
# - Ansible Galaxy dependencies (roles and collections)
# - Git configuration
# - Project directory structure

- name: Set up local development environment
  hosts: localhost
  connection: local
  vars:
    git_email: "{{ email }}"
    git_name: "Jonas Lundberg"
    ansible_root_dir: "{{ playbook_dir }}/.."
  roles:
    - workstation/developer_setup
